# 2026-03-09 ISES-fe 项目全面优化

## 完成的优化工作

### 1. 安全修复
- 移除 `src/config/index.tsx` 中硬编码的服务器 IP 地址 (`43.139.172.162:3000`) 和 AI API URL
- 创建 `.env` 和 `.env.production` 环境变量文件，使用 `VITE_API_BASE_URL` / `VITE_AI_BASE_URL` / `VITE_AI_API_KEY` 管理配置

### 2. Axios 拦截器优化
- 重构 `src/api/index.ts`：增加 401 自动跳转登录、统一错误消息映射表、网络异常/超时处理
- 超时从 5000ms 提升到 15000ms

### 3. 首页 Landing Page 重构
- 移除 Arco Design Button 依赖，使用原生 button + Tailwind CSS
- 移除所有 inline style，替换为 Tailwind class
- 新增渐变遮罩、特性标签、lucide-react 图标
- 增强无障碍属性 (aria-label)

### 4. 404 页面重构
- **移除无用的 AI API 调用**（每次访问404都会触发 GPT 请求）
- 移除外部 GIF 图片依赖
- 使用纯 Tailwind CSS + lucide-react 实现，增加"返回上页"按钮

### 5. 导航 Layout 优化
- resize 监听增加 150ms 防抖
- 用户信息加载时显示 Spin 加载态
- 移除外部 GIF 图片依赖
- 头像区域增加无障碍属性
- useQuery 增加 retry/staleTime 配置

### 6. 发现页修复
- 修复空状态跳转链接错误：`/articles/edit` → `/navigator/publish`

### 7. Campus 页面
- 从 403 占位符改为有意义的"功能建设中"页面，展示即将上线的四个功能预览

### 8. 代码清理
- 清理 5 处 console.log 调试语句
- 重构 zustand store（移除 bears 示例代码，改为 AppState）
- 优化 auth layout inline style
- HTML lang 改为 zh-CN，添加 SEO meta

## 技术栈
- React 18 + TypeScript + Vite + TailwindCSS
- Arco Design + Mantine + lucide-react
- MobX (User/Chat store) + Zustand (App store)
- TanStack Query + Axios
- TipTap 编辑器 + react-spring 动画
- generouted (文件路由)

### 9. 系统样式全面优化（保持主题色 #165dff 不变）

#### tailwind.config.js 扩展
- 新增 `primary` 色阶（50-900），`DEFAULT: "#165dff"`
- 新增 `page: "#f7f8fa"` 统一页面背景色 token
- 新增 `shadow-card` / `shadow-card-hover` 语义化阴影 token

#### index.css 全局样式
- 添加 `scroll-behavior: smooth`、字体抗锯齿
- 统一 `::selection` 选中色为主题蓝
- 优化滚动条样式（WebKit + Firefox `scrollbar-width: thin`）

#### home.css 渐变色修复
- 所有 `#667eea` / `#764ba2` 紫色渐变 → `#165dff` / `#3b82f6` 主题蓝渐变
- 搜索框聚焦增加蓝色边框和阴影

#### editor.css
- `blockquote` 边框色 `#2563eb` → `#165dff`，增加圆角和浅蓝背景
- 链接色 `#007bff` → `#165dff`
- 代码块背景色优化为现代深色 `#1e1e2e`

#### info/style.css
- 搜索框容器和工具箱添加圆角（`border-radius: 12px`）
- 标签 hover 增加蓝色背景
- 表格容器增加圆角和边框

#### publish/style.css
- 编辑器头部/主体增加圆角
- 统一所有圆角为 6px-12px

#### 页面背景色统一
- 所有 `bg-[#F7F8FA]` / `bg-[#f7f8fa]` / `bg-[#f4f6fb]` / `bg-[#f3f6fb]` / `bg-gray-50`
  → 统一为 `bg-page`（token 值 #f7f8fa）
- 涉及: explore、referrals、chat、profile、dashboard、admin、campus、navigator index

#### 颜色主题统一
- 所有 `text-blue-500` / `text-blue-600` / `bg-blue-500` / `bg-blue-600` 交互色
  → 统一为 `text-primary` / `bg-primary` / `bg-primary-600`（主题 #165dff）
- 所有 `hover:text-[#1e80ff]` → `hover:text-primary`
- 所有 `border-[#165DFF]` → `border-primary`
- 涉及: 登录/注册/找回、首页、发现页、热榜、内推、爆料、校园、管理后台

#### 内联样式清理
- CreatorList / CompanyAlumni 的 `style={{ border: '1px solid transparent' }}` → Tailwind class
- Avatar `style={{ color, fontWeight }}` → Tailwind class
- Tag `style={{ borderRadius, fontSize }}` → Tailwind class
- Explore Tag `style={{ border, padding, height, fontSize, borderRadius }}` → Tailwind class

#### 卡片和容器统一
- 所有卡片容器圆角统一为 `rounded-xl`（12px）
- 表格/列表阴影统一为 `shadow-card` / `shadow-card-hover`
- Dashboard 图表边框色 `border-[#e7ebf3]` → `border-gray-200`
- Dashboard 主色 `#2563eb` → `#165dff`

#### 认证通过：TypeScript 编译零错误
