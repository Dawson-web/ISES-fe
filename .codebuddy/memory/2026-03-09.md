# 2026-03-09 ISES-fe 项目全面优化

## 完成的优化工作

### 1. 安全修复
- 移除 `src/config/index.tsx` 中硬编码的服务器 IP 地址 (`43.139.172.162:3000`) 和 AI API URL
- 创建 `.env` 和 `.env.production` 环境变量文件，使用 `VITE_API_BASE_URL` / `VITE_AI_BASE_URL` / `VITE_AI_API_KEY` 管理配置

### 2. Axios 拦截器优化
- 重构 `src/api/index.ts`：增加 401 自动跳转登录、统一错误消息映射表、网络异常/超时处理
- 超时从 5000ms 提升到 15000ms

### 3. 首页 Landing Page 重构
- 移除 Arco Design Button 依赖，使用原生 button + Tailwind CSS
- 移除所有 inline style，替换为 Tailwind class
- 新增渐变遮罩、特性标签、lucide-react 图标
- 增强无障碍属性 (aria-label)

### 4. 404 页面重构
- **移除无用的 AI API 调用**（每次访问404都会触发 GPT 请求）
- 移除外部 GIF 图片依赖
- 使用纯 Tailwind CSS + lucide-react 实现，增加"返回上页"按钮

### 5. 导航 Layout 优化
- resize 监听增加 150ms 防抖
- 用户信息加载时显示 Spin 加载态
- 移除外部 GIF 图片依赖
- 头像区域增加无障碍属性
- useQuery 增加 retry/staleTime 配置

### 6. 发现页修复
- 修复空状态跳转链接错误：`/articles/edit` → `/navigator/publish`

### 7. Campus 页面
- 从 403 占位符改为有意义的"功能建设中"页面，展示即将上线的四个功能预览

### 8. 代码清理
- 清理 5 处 console.log 调试语句
- 重构 zustand store（移除 bears 示例代码，改为 AppState）
- 优化 auth layout inline style
- HTML lang 改为 zh-CN，添加 SEO meta

## 技术栈
- React 18 + TypeScript + Vite + TailwindCSS
- Arco Design + Mantine + lucide-react
- MobX (User/Chat store) + Zustand (App store)
- TanStack Query + Axios
- TipTap 编辑器 + react-spring 动画
- generouted (文件路由)
